# ==============================================================
#  Gramma — Instagram Multi-Page Telegram Bot
#  Core runtime dependencies
# ==============================================================

# ── Telegram Bot framework (aiogram v3) ──────────────────────
aiogram>=3.7,<4

# ── Database (async) ─────────────────────────────────────────
SQLAlchemy>=2.0,<3
asyncpg>=0.29            # PostgreSQL async driver (production)
aiosqlite>=0.20          # SQLite async driver (local/dev fallback)

# ── Task queue / scheduler (Celery + Redis) ──────────────────
celery>=5.3,<6
redis>=5.0

# ── Security: AES-256 / Fernet token encryption ─────────────
cryptography>=42.0

# ── HTTP + config ────────────────────────────────────────────
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3

# ── Logging ──────────────────────────────────────────────────
structlog>=24.1

# ── In-process fallback scheduler (works without Redis, dev) ─
apscheduler>=3.10

# ── Optional: OpenAI-compatible endpoint for AI captions ─────
openai>=1.30

# ── Optional: object storage (S3-compatible) as media CDN ────
#     (Instagram requires publicly reachable media URLs)
boto3>=1.34

# ── Mini App (WebApp) backend: FastAPI + uvicorn ─────────────
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# ── Date/time utilities ──────────────────────────────────────
# (Jalali/Shamsi conversions are implemented in pure Python in
#  app/utils/jalali.py — no babel/jdatetime dependency needed.)
tzdata>=2024.1

# ── Dev / test ───────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
