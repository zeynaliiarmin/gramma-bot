# گزارش تشخیص زنده — ۲۰۲۶-۰۹-۲۲ (جلسه جدید)

> این گزارش فقط شامل یافته‌های تأییدشده با کوئری‌های زنده است (بدون تست Mock).
> هیچ کلیدی در این فایل نیست؛ همه کلیدها فقط در `.env` محلی (gitignored).

## وضعیت تأییدشده (سالم)
- آخرین دیپلویمنت Vercel = commit `5b0ce76` (main)، وضعیت READY، دامنه `miniapp-five-inky.vercel.app` متصل و `/healthz` = 200.
- Supabase: ۲۱ جدول آماده. کاربر `zeynaliarmin` (id=495432021) و پیج `zeynalikids` (status=connected, id=4) وجود دارند.
- توکن workspace چت‌بات‌ایکس معتبر است؛ `GET /api/v1/conversations` با آن جواب می‌دهد.
- پیام‌های واقعی اینستاگرام به چت‌بات‌ایکس می‌رسند: ۳ پیام «قیمت؟» امروز 17:36/17:38/17:41Z در مکالمه `11706294067920897` ثبت شده (واردشده از contact، بدون هیچ پاسخ خروجی).

## ریشه باگ‌ها (با مدرک)
1. **وب‌هوک چت‌بات‌ایکس هیچ پیام واقعی نگرفته**: کل ۱۰ رکورد `activity_logs` از تست‌های Mock (conv=123456) هستند. پیام‌های واقعی در چت‌بات‌ایکس مانده‌اند → کانال API در چت‌بات‌ایکس به `https://miniapp-five-inky.vercel.app/api/chatbotx-webhook` فراخوانی نمی‌شود (تنظیم/فعال نبودن کانال یا URL).
2. **جدول `auto_replies` خالی است** — هیچ قانونی (حتی قیمت) وجود ندارد. حتی اگر وب‌هوک می‌رسید، قانونی برای match نبود.
3. **تاخیر دکمه‌های ربات**: در `api/index.py` تابع وب‌هوک تلگرام اول کامل پردازش می‌کند بعد 200 می‌دهد (process-then-ack، نه ack-first که ادعا شده بود) + ساخت مجدد Bot/Dispatcher + deepcopy از ۱۲ روتر در هر درخواست + واردات تنبل (lazy) هندلرها داخل هر درخواست. نتیجه: هر کلیک دکمه = یک اجرا‌ی سنگین کامل.
4. **هنگ ۳۰ ثانیه‌ای روی OpenClaw**: مسیر هوش مصنوعی اول به `127.0.0.1:18789` وصل می‌شود که از Vercel غیرقابل دسترس است؛ لاگ واقعی `openclaw_comment_reply_error` با timeout=30s ثبت شده.
5. **ارسال پاسخ**: `send_message` از توکن workspace روی `POST /v1/conversations/{id}/messages` استفاده می‌کند؛ این اندپوینت هنوز با پیام واقعی تست نشده (بدون اجازه کاربر تست نمی‌شود چون به اینستاگرام واقعی می‌رود). تلاش‌های قبلی فقط به مکالمه ساختگی 123456 بود (404).
6. **شناسه ورک‌اسپیس غلط**: در پیکربندی `1170629` است ولی API مقدار `11706290428788736` را برمی‌گرداند. هر چیزی که به `workspace_id` وابسته است (مثل وضعیت اتصالات مینی‌اپ) 404 می‌گیرد.
7. **آمار نادرست مینی‌اپ**: حساب اینستاگرام هرگز سینک واقعی نشده (`last_sync_at=null`، بدون داده فالوور)؛ داشبورد از داده‌های شبیه‌سازی‌شده پر شده.
8. `is_admin` برای ادمین در جدول `users` مقدار `false` دارد (در `get_or_create_user` هرگز ست نمی‌شود) — فعلاً جایی گیت نمی‌کند ولی باید اصلاح شود.

## محدودیت‌های محیطی کشف‌شده
- متغیرهای `sensitive` در Vercel از طریق API خواندنی نیستند → نمی‌شود بدون توکن ربات تأیید کرد که `TELEGRAM_BOT_TOKEN` و بقیه کلیدها در production واقعاً مقدار دارند.
- لاگ‌های ران‌تایم Vercel با پلن فعلی از طریق API پس‌ازواقعه قابل بازیابی نیستند؛ کانال اثبات زنده ما `activity_logs` در Supabase است (خود اپ آنجا لاگ می‌نویسد).
- توکن ربات تلگرام در این جلسه موجود نیست (در پرامپت نیامده) → برای `getWebhookInfo` لازم است.

## برنامه اصلاح (پس از تأیید کاربر)
1. وب‌هوک تلگرام: کش Dispatcher/Bot در سطح ماژول (حذف هزینه هر درخواست)، واردات هندلرها در زمان بارگذاری، `callback.answer()` قبل از هر کار دیگر، و حذف کامل مسیرهای بلاک‌کننده (OpenClaw) از مسیر وب‌هوک.
2. غیرفعال‌سازی فراخوانی OpenClaw در `RUN_MODE=webhook`؛ فال‌بک مستقیم به AvalAI با تایم‌اوت کوتاه.
3. ایجاد قانون قیمت (و قوانین پایه) در `auto_replies` پس از تأیید کاربر.
4. اصلاح `workspace_id` به مقدار واقعی و راستی‌آزمایی اندپوینت ارسال با یک تست واقعی روی مکالمه تستی خود کاربر.
5. اتصال آمار داشبورد به داده واقعی (یا نمایش صریح «بدون داده» به جای عدد ساختگی).
6. ست شدن `is_admin` از `ADMIN_TELEGRAM_IDS` هنگام ساخت کاربر.

## تست واقعی لازم (طبق قانون ۴ — هیچ «تمام شد» بدون این‌ها)
- کاربر: `/start` → تاخیر؟ / دکمه‌ها → پاسخ؟ / «پنل مدیریت» → باز شدن مینی‌اپ؟
- کاربر: ارسال دایرکت واقعی «قیمت؟» به پیج → بررسی `activity_logs` برای `chatbotx_inbound` جدید.
- بررسی تنظیمات کانال API در پنل چت‌بات‌ایکس (Callback URL + فعال بودن).

## نکات امنیتی
- اسکن مخزن: هیچ کلیدی در کد/کامیت‌ها نیست (برخورد `cbx_` فقط نام متغیر بود).
- چون کلیدها در متن چت گذشته‌اند، بعد از پایان این فاز چرخاندنشان توصیه می‌شود (به‌ویژه service-role سوپابیس، پسورد دیتابیس، توکن‌های Vercel).

---

## به‌روزرسانی همان روز — فاز اصلاح (Stage 8)

### اصلاحات دیپلوی‌شده (main @ f5c4a3e)
1. **ack-first واقعی**: `AckFirstMiddleware` در وب‌هوک تلگرام قبل از هر کاری `callback.answer()` می‌دهد؛ `CallbackQuery.answer` idempotent شد.
2. **سرعت**: ساخت یک‌باره‌ی Dispatcher در سطح ماژول (بدون ساخت مجدد/واردات تنبل در هر درخواست)؛ فقط Bot سبک در هر درخواست.
3. **حذف هنگ ۳۰ ثانیه‌ای**: کلاینت OpenClaw در محیط Vercel (VERCEL=1) فوراً OpenClawError می‌دهد؛ فال‌بک مستقیم به AvalAI با تایم‌اوت ۵ ثانیه در مسیر وب‌هوک.
4. **قوانین پایه**: ۳ قانون در `auto_replies` برای حساب ۴ (قیمت/سلام/ارسال) — با تست زنده `chatbotx_rule_match` تأیید شد.
5. **is_admin**: ساخت/خودترمیمی از `ADMIN_TELEGRAM_IDS` + رکورد کاربر ادمین اصلاح شد.
6. **صداقت آمار**: اعداد رندوم از داشبورد حذف شد؛ `has_data=false` + «بدون داده» (بک‌اند + فرانت).
7. **باگ پنهان**: `QUEUE_PROCESSING` در monitoring ایمپورت نشده بود → کرون‌جاب‌ها کرش می‌کردند؛ رفع شد.
8. **نقطه‌های تشخیص**: `/api/diag/db` و `/api/diag/env` (محافظت‌شده با X-Cron-Secret).
9. اتصال دیتابیس: رشته اتصال به **پولر تراکنش‌ها** (پورت 6543، `aws-1-eu-west-1`) تغییر کرد — دایرکت 5432 از Vercel کار نمی‌کرد.

### حادثه و بازیابی (مهم — شفاف‌سازی)
هنگام اجرای تست‌های محلی، متغیر محیطی `DATABASE_URL` اشتباهاً روی دیتابیس **پروداکشن** بود و سوئیت تست (که حذف/درج تستی دارد) روی داده واقعی اجرا شد. نتیجه: سطرهای واقعی کاربران/پیج/قوانین حذف شدند.
**بازیابی کامل انجام شد:** کاربر ادمین (495432021)، پیج zeynalikids (id=4, connected)، و ۳ قانون پاسخ خودکار. آنچه قابل بازیابی نبود: تاریخچه `activity_logs` (فقط لاگ‌های تشخیصی/شبیه‌سازی همان روز) و چند سطر شبیه‌سازی در `comment_reply_queue`/`daily_reply_counters`.
**پادزهر دائمی:** `conftest.py` در ریشه مخزن، `DATABASE_URL` را قبل از هر تست به صورت سخت به sqlite قفل می‌کند.

### وضعیت در لحظه تحویل به کاربر
- دیتابیس از تابع وصل است (`/api/diag/db` → `SELECT 1` ok).
- مطابقت قانون قیمت به‌صورت زنده اثبات شد (`قیمت چند؟` → پاسخ قانون).
- وب‌هوک تلگرام با توکن جدید + سکرت تنظیم شده؛ بدون خطا.
- **هنوز اثبات‌نشده**: اینکه چت‌بات‌ایکس پیام واقعی اینستاگرام را به وب‌هوک تحویل می‌دهد یا نه — فقط با تست واقعی کاربر مشخص می‌شود.
