# ── Secrets & environment ───────────────────────────────────
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# ── Python / tooling ────────────────────────────────────────
.venv/
venv/
**/__pycache__/
*.py[cod]
*.db
*.sqlite3
.git/
.gitignore
tests/

# ── Frontend build deps (never needed in the serverless bundle) ──
miniapp/node_modules/

# ── Keep the SPA source + its gitignored dist/ OUT of the Python function
# ── bundle (Vercel serves miniapp/dist as static output). Source files that
# ── exist in git are ignored here, so the Python bundle stays lean.
miniapp/src/
app/webapp/static/

# ── Local-only tooling ───────────────────────────────────────
openclaw/
gramma/
gramma-bridge/
openclaw-e2e/
openclaw-runtime/
README.md
Dockerfile
docker-compose.yml
